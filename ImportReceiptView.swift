import SwiftUI

struct ImportReceiptView: View {
    var body: some View {
        VStack {
            Text("Import Receipt View")
                .font(.title)
                .padding()
            Text("This is a placeholder")
                .font(.subheadline)
                .foregroundColor(.gray)
        }
    }
}
